services:
  db:
    image: postgres:18.1
    environment:
     POSTGRES_USER: postgres
     POSTGRES_PASSWORD: postgres
     POSTGRES_DB: postgres
    ports:
      - 5433:5432
    volumes:
       - postgres:/var/lib/postgresql

volumes:
  postgres:
